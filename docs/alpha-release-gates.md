# Alpha release gates

Completed on the Phase 7.5 release branch: Node 20/22 validation, 65+ regression tests, packed-consumer installation, installed CLI and package import smoke tests, JSON schema v1, npm package dry-run, updated public documentation, and manual audit of all current production findings with zero known false VERIFIED classifications.

Before publication, merge this branch to `main`, install an approved npm publishing workflow, configure npm publishing authentication/trusted publishing, and create the release tag from the final `main` commit.
